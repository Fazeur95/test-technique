import 'package:flutter_test/flutter_test.dart';
import 'package:searchimages/main.dart';

void main() {
  testWidgets('MyApp builds without error', (tester) async {
    await tester.pumpWidget(MyApp());
  });
}
